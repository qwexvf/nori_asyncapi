//// Generated by nori_asyncapi. Handler stubs — fill in the bodies.
//// Source: Chat v1.0.0

import gleam/dict.{type Dict}
import gleam/dynamic.{type Dynamic}
import gleam/option.{type Option}
import generated/types as types

// ---------------------------------------------------------------------------
// channel: rooms/{roomId}
// ---------------------------------------------------------------------------
/// Handle `deleteChat` arriving on `rooms/{roomId}`.
pub fn handle_delete_chat(msg: types.ChatDeleted) -> Nil {
  todo
}

/// Handle `editChat` arriving on `rooms/{roomId}`.
pub fn handle_edit_chat(msg: types.ChatEdited) -> Nil {
  todo
}

/// Emit `onPresence` on `rooms/{roomId}`.
pub fn emit_on_presence(msg: types.Presence) -> Nil {
  todo
}

/// Emit `onRoomEvent` on `rooms/{roomId}`.
pub fn emit_on_room_event(msg: types.RoomEvent) -> Nil {
  todo
}

/// Handle `sendChat` arriving on `rooms/{roomId}`.
pub fn handle_send_chat(msg: types.ChatSent) -> Nil {
  todo
}
